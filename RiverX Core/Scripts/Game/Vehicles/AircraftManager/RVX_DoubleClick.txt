class RVX_DoubleClick: ScriptedWidgetComponent
{
	private ref ScriptInvoker action = new ScriptInvoker();
	
	override bool OnDoubleClick(Widget w, int x, int y, int button)
	{
		action.Invoke(x, y);
		return true;
	}
	
	ScriptInvoker GetOnDoubleClick()
	{
		return action;
	}
}